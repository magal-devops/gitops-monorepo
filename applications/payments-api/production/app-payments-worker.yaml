kubernetes:
  apps:
    - name: payments-worker
      image:
        repository: nginx
        registry: docker.io
        pullPolicy: IfNotPresent
        tag: latest
      kind: Deployment
      
      # Worker não precisa de service externo
      service:
        enabled: false
      
      # Configurações específicas do worker
      replicas: 2
