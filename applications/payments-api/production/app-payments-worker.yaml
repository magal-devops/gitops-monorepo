kubernetes:
  apps:
    - name: payments-worker
      image:
        repository: nginx
        registry: docker.io
        pullPolicy: IfNotPresent
        tag: latest
      kind: Deployment

      
      # Configurações específicas do worker
      replicas: 2
