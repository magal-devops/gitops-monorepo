kubernetes:
  apps:
    - name: payments-api
      image:
        repository: nginx
        registry: docker.io
        pullPolicy: IfNotPresent
        tag: latest
      kind: Deployment
      
      replicas: 1
